<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.waterwaveview">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/wave"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Consults"></activity>
        <activity android:name=".RetrievePasswordRight" />
        <activity android:name=".RetrievePassword" />
        <activity android:name=".SettingPassword" />
        <activity android:name=".Register_Input_VerificationCode" />
        <activity android:name=".Register_Input_PhoneNumber" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/button_shape_normal" />

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>